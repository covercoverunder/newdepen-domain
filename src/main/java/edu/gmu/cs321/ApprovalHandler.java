package edu.gmu.cs321;

/**
 * Last validation check for application.
 * If passess validation, application is marked as approved and ready for next process.
 * Otherwise, the application is denied and must revert back to previous step.
 * @author Michael Toyco
 */
public class ApprovalHandler {

    /* ------------------- operators ------------------- */

    /**
     * boolean approveForm().
     * If document passess all check/validation,
     * application is marked as approved and
     * ready for next steps. Otherwise, it is
     * denied and returns to previous procedure
     * for further evaluation.
     * @return If it passess validation check, it returns true. Otherwise, false.
     */
    public boolean approveForm() {
        return false;
    }
}
