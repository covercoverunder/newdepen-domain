package edu.gmu.cs321;

/**
 * Checks if form filled by petitioner is valid.
 * If all fields are valid, is it processed for 
 * review. Otherwise, prompts petitioner for 
 * with error message.
 * @author Michael Toyco
 */
public class EntriesHandler {

    /* ------------------- attributes ------------------- */

    /**
     * boolean processForReview.
     * If application passess all validation test,
     * it is processed for further evaluation.
     * @return If application passes all test, it returns true. Otherwise, false.
     */
    public boolean processForReview() {
        return false;
    }    
}
